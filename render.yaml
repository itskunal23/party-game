services:
  - type: web
    name: party-game
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NVIDIA_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
